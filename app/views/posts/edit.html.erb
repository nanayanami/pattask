<h1>編集する</h1>
<%= form_with model: [@team, @category, @post], local: true do |f| %>
  <h4>タイトル</h4>
  <%= f.text_field :title %>
  <h4>内容</h4>
  <%= f.text_area :post %>
  <%= f.submit "更新する" %>
<% end %>