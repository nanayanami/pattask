<h1><%= @category.name %></h1>
<p>チーム: <%= @team.name %></p>

<%= link_to "新規投稿", new_team_category_post_path(@team, @category) %>
<%= link_to "カテゴリー一覧に戻る", team_categories_path(@team) %>

<h2>投稿一覧</h2>
<% if @category.posts.any? %>
  <% @category.posts.each do |post| %>
    <div>
      <h3><%= post.title %></h3>
      <p><%= post.post %></p>
    </div>
  <% end %>
<% else %>
  <p>まだ投稿がありません。</p>
<% end %>
