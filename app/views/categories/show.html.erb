<h1><%= @category.name %></h1>
<p>チーム: <%= @team.name %></p>

<%= link_to "カテゴリー名を編集する",  edit_team_category_path(@team, @category) %><br>
<%= link_to 'カテゴリーを削除する', team_category_path(@team, @category),  data: { turbo_method: :delete, turbo_confirm:  "本当に消しますか？"} %><br>
<%= link_to "本カテゴリー内における新規投稿", new_team_category_post_path(@team, @category) %><br>
<%= link_to "カテゴリー一覧に戻る", team_categories_path(@team) %><br>
<%= link_to "チーム一覧に戻る", teams_path %><br>



<h2>投稿一覧</h2>
<% if @category.posts.any? %>
  <% @category.posts.each do |post| %>
    <div>
      <h3><%= link_to post.title, team_category_post_path(@team, @category, post) %></h3>
      <p><%= truncate(post.post, length: 100) %></p>
    </div>
  <% end %>
<% else %>
  <p>まだ投稿がありません。</p>
<% end %>
